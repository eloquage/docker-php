<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Connection mode
    |--------------------------------------------------------------------------
    | One of: "unix", "tcp", "tls". Unix uses a local socket; tcp/tls use host:port.
    */
    'connection' => env('DOCKER_CONNECTION', 'unix'),

    /*
    |--------------------------------------------------------------------------
    | Unix socket path (connection = unix)
    |--------------------------------------------------------------------------
    */
    'unix_socket' => env('DOCKER_UNIX_SOCKET', '/var/run/docker.sock'),

    /*
    |--------------------------------------------------------------------------
    | Host (connection = tcp or tls)
    |--------------------------------------------------------------------------
    */
    'host' => env('DOCKER_HOST', 'localhost'),

    /*
    |--------------------------------------------------------------------------
    | Port (connection = tcp or tls)
    |--------------------------------------------------------------------------
    */
    'port' => env('DOCKER_PORT', 2375),

    /*
    |--------------------------------------------------------------------------
    | API version
    |--------------------------------------------------------------------------
    | Docker Engine API version (e.g. v1.53).
    */
    'version' => env('DOCKER_API_VERSION', 'v1.53'),

    /*
    |--------------------------------------------------------------------------
    | TLS certificate paths (connection = tls)
    |--------------------------------------------------------------------------
    */
    'tls_cert' => env('DOCKER_TLS_CERT'),
    'tls_key' => env('DOCKER_TLS_KEY'),
    'tls_ca' => env('DOCKER_TLS_CA'),
    'tls_verify' => env('DOCKER_TLS_VERIFY', true),

    /*
    |--------------------------------------------------------------------------
    | Timeouts (seconds)
    |--------------------------------------------------------------------------
    */
    'connect_timeout' => (int) env('DOCKER_CONNECT_TIMEOUT', 10),
    'timeout' => (int) env('DOCKER_TIMEOUT', 30),

    /*
    |--------------------------------------------------------------------------
    | Default headers
    |--------------------------------------------------------------------------
    */
    'headers' => [
        'User-Agent' => env('DOCKER_USER_AGENT', 'Docker-PHP-Client/1.0'),
    ],

    /*
    |--------------------------------------------------------------------------
    | Cache (for read-only API responses)
    |--------------------------------------------------------------------------
    */
    'cache' => [
        'enabled' => env('DOCKER_PHP_CACHE_ENABLED', true),
        'store' => env('DOCKER_PHP_CACHE_STORE'),
    ],

    /*
    |--------------------------------------------------------------------------
    | Dashboard UI
    |--------------------------------------------------------------------------
    */
    'ui' => [
        'enabled' => env('DOCKER_PHP_UI_ENABLED', true),
        'prefix' => env('DOCKER_PHP_UI_PREFIX', 'docker'),
        'middleware' => ['web'],
    ],
];
